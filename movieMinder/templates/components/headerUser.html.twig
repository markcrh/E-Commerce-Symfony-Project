<header>
	<nav class="bg-transparent w-full z-20 top-0 start-0">
		<div class="w-full flex flex-wrap items-center justify-between mx-auto p-4">
			<a href="{{ path('app_home') }}" class="flex items-center space-x-3 rtl:space-x-reverse">
				<img src="{{ asset('img/logoMovieMinder.svg') }}" class="h-12" alt="MovieMinder Logo">
			</a>

			<div class="items-center justify-between hidden w-full md:flex md:w-auto md:order-1 text-white" id="navbar-sticky">
				<ul class="flex flex-col p-4 md:p-0 mt-4 items-center font-medium border border-gray-100 rounded-lg md:space-x-8 rtl:space-x-reverse md:flex-row md:mt-0 md:border-0">
					<li>
						<a href="{{path('app_movie_index')}}">
							<h2 class="text-white text-2xl">ALL MOVIES</h2>


						</a>

					</li>
					<li>
						<a href="{{ path('user_dashboard') }}" class="text-xl block py-2 px-3 rounded md:bg-transparent
																					                                  {{ app.request.attributes.get('_route') == 'user_dashboard' ? 'text-orange-500' : 'text-white hover:text-orange-500' }}
																					                                  md:p-0" aria-current="{{ app.request.attributes.get('_route') == 'user_dashboard' ? 'page' : '' }}">MY LISTS</a>
					</li>
					<li>
						<a href="#" class="block text-xl py-2 px-3 rounded text-white hover:text-orange-500 md:p-0">
							MY SERIES
						</a>
					</li>
					<li>
						<a href="{{ path('show_list') }}" class="block text-xl py-2 px-3 rounded text-white hover:text-orange-500 md:p-0
														{{ app.request.attributes.get('_route') == 'show_list' ? 'text-orange-500' : 'text-white hover:text-orange-500' }}" aria-current="{{ app.request.attributes.get('_route') == 'show_list' ? 'page' : '' }}">MY MOVIES</a>

					</li>
<li
	class="relative">
	
	<button id="user-btn" type="button" class="flex items-center gap-1 text-black bg-orange-500 hover:text-2xl focus:ring-4 focus:outline-none focus:ring-blue-300 font-extrabold rounded-2xl text-xl px-4 py-2 text-center">
		<img src="{{ asset('img/POP.svg') }}" class="h-8" alt="User Icon">
		{{ app.user.username }}
	</button>

	
	<div id="user-dropdown" class="absolute right-0 mt-2 w-48 bg-white text-black shadow-lg rounded-lg hidden">
		<ul class="py-2">
			<li>
				<a href="{{ path('app_logout') }}" class="block px-4 py-2 text-sm hover:bg-gray-100">
					Logout
				</a>
			</li>
		</ul>
	</div>
</li>

<script>
	document.getElementById('user-btn').addEventListener('click', function () {
var dropdown = document.getElementById('user-dropdown');
dropdown.classList.toggle('hidden');
});
</script>


				</ul>
			</div>
		</div>
	</nav>
</header>
